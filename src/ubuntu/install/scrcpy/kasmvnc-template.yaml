desktop:
  resolution:
    width: ${SCRCPY_WIDTH}
    height: ${SCRCPY_HEIGHT}
  pixel_depth: 24
  allow_resize: false
runtime_configuration:
  allow_client_to_override_kasm_server_settings: false
encoding:
  max_frame_rate: ${SCRCPY_FPS}
  video_encoding_mode:
    max_resolution:
      width: ${SCRCPY_WIDTH}
      height: ${SCRCPY_HEIGHT}
